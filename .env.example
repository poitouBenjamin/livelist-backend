# Copy this file to .env.local and fill in real values.
# .env.local must never be committed (already covered by .gitignore).

DATABASE_URL="postgresql://livelist:livelist_dev_password@localhost:5432/livelist"

# Auth.js / Google provider — filled in once auth is set up
# AUTH_SECRET=
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=